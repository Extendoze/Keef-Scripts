local XanaxUILib = loadstring(game:HttpGet("https://pastebin.com/raw/XZz3Ytbu"))()
local Ui = XanaxUILib:CreateWindow("Fs Hub")
local ExampleSection = Ui:CreateSection("Main")
local EarthTP = Ui:CreateSection("Earth Locations")
local Info = Ui:CreateSection("Info")
 
Info:CreateLabel("Keef#0001")
Info:CreateLabel("FS Hub")
Info:CreateButton("Destroy UI", function() XanaxUILib.functions:DestroyGUI() end)


EarthTP:CreateLabel("Earth Locations")
ExampleSection:CreateLabel("FS TP Features")
EarthTP:CreateButton("South City", function() print("")
    local tweenservice = game:GetService("TweenService")

local player = game:GetService("Players").LocalPlayer
local char = player.Character

local tweeninfo = TweenInfo.new(1.5, Enum.EasingStyle.Quad)
local cframe = {CFrame = CFrame.new(-473, 27, -6406)}
tweenservice:Create(char.HumanoidRootPart, tweeninfo, cframe):Play()
    end)
EarthTP:CreateButton("West City", function() print("")
    local tweenservice = game:GetService("TweenService")

local player = game:GetService("Players").LocalPlayer
local char = player.Character

local tweeninfo = TweenInfo.new(1.5, Enum.EasingStyle.Quad)
local cframe = {CFrame = CFrame.new(-578, 22, -2878)}
tweenservice:Create(char.HumanoidRootPart, tweeninfo, cframe):Play()
    end)
EarthTP:CreateButton("Central City", function() print("") 
    local tweenservice = game:GetService("TweenService")

local player = game:GetService("Players").LocalPlayer
local char = player.Character

local tweeninfo = TweenInfo.new(1.5, Enum.EasingStyle.Quad)
local cframe = {CFrame = CFrame.new(-3438, 165, -1705)}
tweenservice:Create(char.HumanoidRootPart, tweeninfo, cframe):Play()
end)

ExampleSection:CreateButton("SW | 350 Req", function() print("")
    game:GetService("TeleportService"):Teleport(2046990924, game.Players.LocalPlayer)

    end)

ExampleSection:CreateButton("Queue", function() print("")
    game:GetService("TeleportService"):Teleport(3565304751, game.Players.LocalPlayer)

end)

ExampleSection:CreateButton("Earth", function() print("") 
   
game:GetService("TeleportService"):Teleport(536102540, game.Players.LocalPlayer)
end)


ExampleSection:CreateButton("Space | lvl 200 Req", function() print("") 
game:GetService("TeleportService"):Teleport(478132461, game.Players.LocalPlayer)

    end)
ExampleSection:CreateButton("Future | Lvl 300 Req", function() print("")
 game:GetService("TeleportService"):Teleport(569994010, game.Players.LocalPlayer)
end)

ExampleSection:CreateButton("Namek | Lvl 100 Req", function() print("")
game:GetService("TeleportService"):Teleport(882399924, game.Players.LocalPlayer)
end)
EarthTP:CreateButton("Top", function() print("")
    local tweenservice = game:GetService("TweenService")

local player = game:GetService("Players").LocalPlayer
local char = player.Character

local tweeninfo = TweenInfo.new(1.5, Enum.EasingStyle.Quad)
local cframe = {CFrame = CFrame.new(2511, 3945, -2032)}
tweenservice:Create(char.HumanoidRootPart, tweeninfo, cframe):Play()
    end)
    EarthTP:CreateButton("Hard Top", function() print("")
     local tweenservice = game:GetService("TweenService")

local player = game:GetService("Players").LocalPlayer
local char = player.Character

local tweeninfo = TweenInfo.new(1.5, Enum.EasingStyle.Quad)
local cframe = {CFrame = CFrame.new(2508, 3945, -2521)}
tweenservice:Create(char.HumanoidRootPart, tweeninfo, cframe):Play()   
    end)
    
    EarthTP:CreateButton("Broly Pad", function() print("")
    local tweenservice = game:GetService("TweenService")

local player = game:GetService("Players").LocalPlayer
local char = player.Character

local tweeninfo = TweenInfo.new(1.5, Enum.EasingStyle.Quad)
local cframe = {CFrame = CFrame.new(2734, 3945, -2271)}
tweenservice:Create(char.HumanoidRootPart, tweeninfo, cframe):Play()
    end)
